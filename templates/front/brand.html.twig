{% extends "front/base.html.twig" %}

{% block main %}

	<h2>{{brand.name}}</h2>
	<h2>{{brand.description}}</h2>

	{% for product in brand.products %}
		<h4>{{product.name}}</h4>
	{% endfor %}
	



{% endblock %}